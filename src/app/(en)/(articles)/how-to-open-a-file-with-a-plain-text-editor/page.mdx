export const metadata = {
  title: 'How to open a file with a plain text editor',
}

# How to open a file with a plain text editor

## Windows

You can use Notepad.

## Mac

You can use TextEdit. After opening the file, set the editor to plain text mode via `Format -> Make Plain Text`.
